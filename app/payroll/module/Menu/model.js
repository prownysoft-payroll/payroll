define([
		'backbone',
		'namespace',
], function(Backbone, ns, Model) {
	ns.define('payroll.module.menu');
	payroll.module.menu.Model = Backbone.Model.extend({});
	return payroll.module.menu.Model;
});